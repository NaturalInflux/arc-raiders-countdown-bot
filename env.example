# Arc Raiders Countdown Bot Environment Configuration
# Copy this file to .env and fill in your actual values

# Discord Bot Configuration
# Get your bot token from: https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Discord Channel Configuration
# Right-click on the channel in Discord and select "Copy Channel ID"
# Enable Developer Mode in Discord settings first if you don't see this option
CHANNEL_ID=your_channel_id_here

# Game Release Configuration
# Arc Raiders release date (default: October 30, 2025)
# Format: YYYY-MM-DDTHH:mm:ssZ (UTC timezone)
RELEASE_DATE=2025-10-30T00:00:00Z

# Posting Schedule Configuration
# Cron schedule for daily posts (default: 12:00 PM UTC daily - optimal for EU/NA)
# Format: minute hour day month dayOfWeek
# Examples:
#   '0 12 * * *'    - 12:00 PM UTC daily (recommended for EU/NA overlap)
#   '0 9 * * *'     - 9:00 AM UTC daily
#   '0 15 * * *'    - 3:00 PM UTC daily
#   '0 12 * * 1-5'  - 12:00 PM UTC weekdays only
# 
# Time zone breakdown for 12:00 PM UTC:
# - 8:00 AM EST (US East Coast - morning commute)
# - 5:00 AM PST (US West Coast - early morning)
# - 1:00 PM CET (Central Europe - lunchtime)
# - 2:00 PM EET (Eastern Europe - afternoon)
# - 1:00 PM BST (UK - lunchtime)
POST_SCHEDULE=0 12 * * *

# Timezone for posting schedule (default: UTC)
# Common timezones: UTC, America/New_York, Europe/London, Asia/Tokyo
POST_TIMEZONE=UTC

# Reddit API Configuration
# Subreddit to fetch posts from (default: arcraiders)
REDDIT_SUBREDDIT=arcraiders

# Reddit API OAuth Credentials
# Get these from: https://www.reddit.com/prefs/apps
# Create a new "script" type application
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
REDDIT_USERNAME=your_reddit_username_here
REDDIT_PASSWORD=your_reddit_password_here

# Number of Reddit posts to fetch for selection (default: 1 - only top post of the day)
REDDIT_POST_LIMIT=1

# Reddit post filtering
# Maximum length for post text content (default: 500 characters)
REDDIT_MAX_TEXT_LENGTH=500

# Minimum title length for Reddit posts (default: 10 characters)
REDDIT_MIN_TITLE_LENGTH=10

# Bot Configuration
# User agent string for Reddit API requests
REDDIT_USER_AGENT=ArcRaidersCountdownBot/1.0.0

# API request timeout in milliseconds (default: 10000)
API_TIMEOUT=10000

# Number of retry attempts for failed API calls (default: 3)
API_RETRY_ATTEMPTS=3

# Logging Configuration
# Log level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# Environment
# Set to 'production' for production deployment
NODE_ENV=development
